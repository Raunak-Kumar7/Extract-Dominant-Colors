# Extract-Dominant_Colors
 it helps to extract the dominant colors from an image. It gives output in the form of RGB values, shows the colors on a plot and the coressponding hexadecimal and hsv values
